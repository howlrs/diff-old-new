# KPI K2: active session 開始時 Oracle ワープギャップ — `xyz:SP500`

v3 design §3 KPI 2. closure → active 切替時の HL内部mid vs 直後 oracle 平均の bps ギャップ.

- transitions: 0  /  with gap: 0

## 解釈ガイド

- **ギャップ symbol**: 正なら HL内部 < CME (closure 中過小評価), 負なら過大評価
- **ギャップが大きい (>10 bps)** prev_regime は戦略 H1 の優先ターゲット
- **mean が ~0 で std 大** はファットテール → 単純な mean reversion でなく分布 tail を狙う必要

## 次のステップ

1. 1 週間 collect 後に R3 (CMEメンテ) → R1 transition で N >= 5 を取得
2. 1ヶ月で R2 (週末) → R1 transition で N=4 → 累積で十分な統計
3. ギャップ分布の自相関 (consecutive transitions の独立性) を確認